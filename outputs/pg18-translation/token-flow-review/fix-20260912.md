# 審查缺口修正

- 舊 `publish-inbox-structural` 入口現在只執行 dry-run 預檢。不 commit、不勾選、不移動 inbox；正式發佈仍須独立語意審查。
- 控制器掃描 `_published` 中 `by=publish-inbox-structural` 的舊紀錄（本次找到 39 筆）。即使 SQLite 原先已是 done，也會重新標成持久化的 `review_required`。
- 每輪優先補審一頁。從原提交的 parent 取回翻譯前原文，核對 baseline、候選稿與 commit 雜湊，使用原有隔離草稿與批次獨立審查。不更改正式譯文、不建立重複翻譯 commit。
- 通過後保存逐節證據、官方來源資訊、輸入／政策指紋，才改為 done。輸入或政策變動會使通過紀錄失效。
- 失敗會保留待補審狀態與問題；同一輸入的已知失敗不重複呼叫模型。正式稿或快照已变動時要求明確對帳，不覆蓋使用者內容。
- 控制器的正常續跑入口 `python scripts/translation_pipeline.py run` 已接上這段流程；本次沒有啟動正式模型補審或背景控制器。已有未提交的頁面、pending 與 SUMMARY 暫存檔均保留。

驗證：translation 59 項、handoff 22 項、新增 deferred 4 項，共 85 項。新案例涵蓋預檢不發佈、done 重開且跨連線持久化、拒絕不能完成／不反覆重試、通過快取及內容變動失效。模型回應為隔離測試替身，不代表正式 39 頁已審查通過，也不是 token A/B 實測。

尚未 commit：工作目錄有先前 Claude 與使用者的混合變更，本次未將其一併提交。
